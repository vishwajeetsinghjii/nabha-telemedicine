const http=require('http');
function get(url){return new Promise((resolve,reject)=>{const r=http.get(url,res=>{let b='';res.on('data',c=>b+=c);res.on('end',()=>resolve({status:res.statusCode,body:b}));});r.on('error',reject);r.setTimeout(5000,()=>{r.destroy(new Error('timeout'));});});}
(async()=>{console.log('--- Nabha production smoke checks ---');for(const [name,url] of [['backend health','http://localhost:5000/health'],['ai health','http://localhost:8001/health']]){try{const r=await get(url);console.log(`${name}: HTTP ${r.status}`);if(r.status!==200)process.exitCode=1;}catch(e){console.error(`${name}: FAILED`,e.message);process.exitCode=1;}}console.log('Authentication smoke tests intentionally require real provisioned credentials and are not seeded by this script.');})().catch(e=>{console.error(e);process.exitCode=1;});
