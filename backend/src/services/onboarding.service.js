const authService=require('./auth.service');const doctorApp=require('../repositories/doctorApplication.repository');const userRepo=require('../repositories/user.repository');const {NotFoundError}=require('../utils/errors');
class OnboardingService{registerPatient(d){return authService.registerPatient(d)}applyDoctor(d){return authService.registerProfessional({...d,role:'DOCTOR'})}listDoctorApplications(status){return doctorApp.findAll(status)}async getDoctorApplicationById(id){const a=await doctorApp.findById(id);if(!a)throw new NotFoundError('Doctor application');return a}async approveDoctor(adminUserId,id,notes=''){const a=await doctorApp.findById(id);if(!a)throw new NotFoundError('Doctor application');const updated=await doctorApp.updateStatus(id,'APPROVED',adminUserId,notes);await userRepo.updateAccountStatus(a.userId,'ACTIVE');return{application:updated,accountStatus:'ACTIVE'}}async rejectDoctor(adminUserId,id,notes=''){const a=await doctorApp.findById(id);if(!a)throw new NotFoundError('Doctor application');const updated=await doctorApp.updateStatus(id,'REJECTED',adminUserId,notes);await userRepo.updateAccountStatus(a.userId,'REJECTED');return{application:updated,accountStatus:'REJECTED'}}}
module.exports=new OnboardingService();
