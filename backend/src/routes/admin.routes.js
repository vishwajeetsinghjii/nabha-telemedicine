const express=require('express');const router=express.Router();const c=require('../controllers/admin.controller');const auth=require('../middleware/auth.middleware');const roles=require('../middleware/role.middleware');router.use(auth,roles.requireRoles('ADMIN'));router.get('/verification',c.listVerification);router.get('/verification/history',c.verificationHistory);router.get('/audit',c.auditHistory);router.get('/consultation-requests',c.consultationRequests);router.get('/consultation-requests/doctors',c.consultationDoctors);router.post('/consultation-requests/:id/assign',c.assignConsultationDoctor);router.patch('/users/:userId/approve',c.approveUser);router.patch('/users/:userId/reject',c.rejectUser);router.get('/doctor-applications/:applicationId/documents',c.doctorApplicationDocuments);router.get('/doctor-applications/:applicationId/documents/:documentId',c.downloadDoctorDocument);module.exports=router;
